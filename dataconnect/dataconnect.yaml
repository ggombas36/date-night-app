specVersion: "v1"
serviceId: "date-night-app"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "date-night-app-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
